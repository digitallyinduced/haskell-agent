# agent-responses-types

Canonical, lossless wire types for Responses-compatible model APIs.

This package contains only the shared request, response, item, tool, and
streaming-event data model. Transport codecs and agent-loop adapters live in
`agent-responses`.
