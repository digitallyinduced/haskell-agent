# agent-grok-build-dialect

The Grok Build model-facing contract for the universal agent harness: prompts,
tool schemas and handlers, persistent shell behavior, task conventions, and
project-instruction formatting. Provider transport and authentication remain
in `agent-xai`.
